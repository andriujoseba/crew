panel=claude-bot-andresmgsl codex-bot-andresmgsl kimi-bot-andresmgsl
triage-actors=dan-claude-bot
scope:cli|C5DEF5|cli/crew — the operator command surface (init, up, profiles, new, console)
scope:duty|C5DEF5|shared/bin and shared/lib — the duty lifecycle engine
scope:prompts|C5DEF5|shared/prompts — the role prompt texts
scope:conf|C5DEF5|shared/conf — role, agent-profile and default configuration
scope:installer|C5DEF5|the install channel — root install.sh, shared/install.sh and dist/
scope:ci|C5DEF5|ci-shell.yml and ci-floor.yml — the engine test gates
scope:fleet-floor|C5DEF5|fleet-floor — the fleet dashboard, its collector and page
scope:drill|C5DEF5|drill/ — the rehearsal harness
scope:protocols|C5DEF5|protocols/ — the claim-race and its siblings, the cross-box invariants
scope:examples|C5DEF5|examples/ — operator config scaffolding and compatibility defaults
scope:docs|C5DEF5|README, shared/docs and the .github entry templates — the prose contract
scope:labels|C5DEF5|the labels workflow, the reconciler, labels.conf and labeler.yml
scope:release|C5DEF5|the release flow — release.yml, CHANGELOG, changelog.d, VERSION, drills, .ceremony

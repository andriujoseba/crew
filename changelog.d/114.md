### Fixed

- The review duty no longer auto-approves over a standing request-changes: the re-request auto-approve now fires only when my covering verdict at that head is an `APPROVED`. A re-request landing over a live `CHANGES_REQUESTED` (or a dismissed verdict) queues a real review round instead of rubber-stamping it, and `submit-verdict.sh` admits that round's considered verdict at the unchanged head (re-keyed to `(me, PR, head, round)`) rather than refusing it as already-present — a bare re-post with no intervening re-request is still refused (#114)

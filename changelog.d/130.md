### Added
- The engine sets `state:addressing` when a round closes without full approval, and requests the panel — setting `state:bots-reviewing` — once a PR's head is green; no session act is needed for a PR to enter or close a review round (#130).

### Changed
- Panel request and re-request move off the builder session into the engine. Every engine label write is optimistic and best-effort; the reconciler stays authoritative and corrects any write it would refuse (#130).

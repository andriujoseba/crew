### Fixed
- A review re-request at an unchanged head is serviced instead of silently skipped: the reviewer's head-dedup rule now also asks whether the request postdates its own verdict, the same test `rereq_decision` already makes before waking it. An issue amendment or a ruling can change the right verdict without changing a commit (#178).

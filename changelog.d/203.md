### Fixed

- The fleet floor's console names a paused box PAUSED and a disarmed one
  DISARMED in the headline, the CRON vital, the big card and the status line —
  SILENT is reserved for a box that should be ticking and is not (#203).
- The floor's alert-coloured `silent` counter and its stage counts count only
  those boxes, so a disarmed fleet no longer reads as a fleet-wide alarm
  (#203).

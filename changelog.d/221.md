### Fixed

- `crew status <box>` reads `no duty log yet` on a box hired minutes ago,
  naming the engine and the tick it waits for, instead of `(unreachable)` (#221).
- The same view on an un-hired box names the absent engine rather than the
  missing log (#221).

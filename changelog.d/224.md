### Fixed

- `crew status` prints the whole roster when a hired box has no duty log yet, instead of stopping at the first one (#224).
- That box's row reads `no ticks yet` — the fallback existed but could never render (#224).

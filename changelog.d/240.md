### Fixed

- The kimi profile launches its CLI non-interactively, so a review session returns a verdict instead of timing out on an approval prompt (#240).
- The kimi credential home is resolved across `~/.kimi` and `~/.kimi-code`, ending the false vendor-credential warning on every tick (#240).

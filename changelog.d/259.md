- Adopt ceremony 0.4.1 (crew#259): the labels sweep detaches from PR-triggered
  runs — a new `labels-sweep.yml` caller carries the hourly cron and the
  bootstrap dispatch (relocated from `labels.yml`, which gains `actions:
  write` for the trigger dispatch), so a queue-displaced sweep can no longer
  land a cancelled `reconcile` check on a PR or set `blocker:ci-red` off its
  own displaced run (ceremony#208/#209).

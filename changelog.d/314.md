### Fixed

- Resume dispatches only when a draft's head, a foreign comment or review, or its referenced issue has moved since the last resume — a parked draft no longer wakes a session every tick forever. The builder's own comments are excluded, or the flood re-arms the gate (#314).
- `shared/prompts/resume.txt` posts the resume marker only when the session is going to act on the draft, which is what BUILDER.md already required of a parked builder (#314).
- A resume that makes no commit at one head three times running is suppressed until the head moves, with one WARN in `duty.log` naming the PR, the head and the count (#314).

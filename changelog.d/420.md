### Added

- The app rehearsal now reads the floor surfaces this release shipped: the serving host's own version, each engine's integrity verdict, the roster boxes counted but not drawn, the repair verb a floor with nothing hired names, and the split between deliberately disarmed and genuinely silent boxes (#420).
- The app rehearsal now asserts `crew up --dry-run` changes nothing, that an unanswered engine probe reads `unknown` rather than never-hired, and that `no build duty` names a cause and a count (#420).

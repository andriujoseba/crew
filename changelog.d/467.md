### Fixed

- `acted=` on the claude and grok agent profiles is a real measurement: both had no working session detector, so every session either ran reported `unknown` and the fleet's idle count was `0` across all 4150 of them (#467).
- A session that hit a usage cap, or whose CLI died before the model spoke, reports `acted=unknown` rather than counting as work (#467).

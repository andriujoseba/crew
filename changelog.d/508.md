### Changed

- `fleet-floor/server/floor.py` is split into a `floor` package on the section banners it already drew — `ping`, `roster`, `units`, `fleet`, `actions`, `alerts`, `server` — with `floor.py` unchanged as the entry point (#508).
- `fleet-floor/test/` mirrors the collector's modules: `cases.sh` becomes one suite per module under `test/floor/` (#508).

### Fixed

- Reviewer ticks reclaim finished review scratch without reading the PR's state, so a retried review always finds its path free (#606, #597).
- A dirty review worktree is preserved by moving it to `kept-<name>-<head>` rather than left on the path the next review needs (#606).

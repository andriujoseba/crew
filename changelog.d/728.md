### Changed

- A builder box reads its identity's authored pull requests only in `repos.txt`
  repositories, never across the whole account (#728).

### Removed

- The `authored PR(s) outside repos.txt` warning (#728).

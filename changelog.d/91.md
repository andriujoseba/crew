### Changed

- handoff no longer spends an agent session or a repository clone: the engine requests the human's review, sets `state:needs-human`, and posts a factual handoff comment (approvals at the current head, the head SHA, and a pointer to the PR body's Round log) itself. The closing prose it used to reconstruct at the end now lives in the PR body's `## Round log`, mirrored there mechanically from each whole-round reply — so no model is spent at handoff (#91)

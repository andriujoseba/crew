### Changed

- Handoff no longer spends an agent session or repository clone: the engine requests human review, sets `state:needs-human`, and posts the current-head approvals and SHA itself (#91)
- The authored handoff record now lives in the PR body's `## Round log`, mirrored mechanically from each whole-round reply (#91)

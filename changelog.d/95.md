### Added

- `install.sh` installs `crew` per-user in a versioned layout, refuses root, converges on re-run, and flips the `current` symlink while reporting engine skew for a new version (#95)
- `cli/crew` resolves its root through the `current` symlink, so it runs the same from a checkout or an install (#95)

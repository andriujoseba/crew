# Repos the triage loop (duty.sh) and hygiene.sh sweep.
#
# Was ceremony-only by doctrine: triage's authority is the process repo. Widened
# on 2026-07-23 at danmt's request, after incubator #35/#36 were filed with
# `needs-triage` and no poller ever looked at that board — the sherpa funnel
# deposited them somewhere its other end did not drain. dan-claude-bot holds
# triage (not push) on incubator, which is enough to label, comment, and close.
#
# Adding a product repo means triage now normalizes a board whose builders own
# it. Keep that in mind before adding the next one.
#
# rig added 2026-07-23 after a full manual triage pass over its board (9 open
# issues normalized, 3 closed, 3 discussions filed). Two things that pass
# learned, both of which the loop now depends on:
#   - Discussions were DISABLED on rig until that day, so the doctrinal funnel
#     had no door: every issue on the board had been filed directly. danmt
#     enabled them mid-pass. A repo joining this list should be checked for
#     that first — triage's first outcome is "reply on the discussion", and
#     without discussions the only outcomes left are label, normalize or close.
#   - dan-claude-bot holds `triage` on rig: it can label, comment, close and
#     retitle, and it can edit the BODY of issues it authored — but not issues
#     authored by anyone else (rig#95 returned "does not have permission to
#     update the issue"). Where a body edit is refused, the normalization goes
#     in a comment that says it is part of the contract.
heavy-duty/ceremony
heavy-duty/incubator
heavy-duty/rig

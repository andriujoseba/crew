# Release drill — 0.1.1 — 2026-08-03

**No real-host drill was run for `0.1.1`.** This file is the waiver
[`drills/README.md`](README.md) asks for — *"say plainly that the drill was
WAIVED and why"* — and it states the measurement that decides whether the
waiver is defensible rather than asserting the conclusion. The record is
required; a passing result is not.

Candidate ref: `crew@29aab8b6f35daa25dbe1c0c16b3d35d196f79250` (`main`,
2026-08-03T13:18:28Z, the merge of PR #320). That is the tree this cut
assembles from, and it is the engine `0.1.1` releases: the release commits
change `VERSION`, `CHANGELOG.md`, `changelog.d/` and this file, and nothing
else. **No file under `shared/`, `cli/`, `drill/`, `fleet-floor/` or
`examples/` differs between the tree measured below and the released one** —
the same claim `drills/0.1.0.md` makes for its own release commit, and it is
checkable from this PR's own diff.

## Why no drill ran

The instrument is [`drill/rehearsal.sh`](../drill/rehearsal.sh) and its
companions, and what they do is mint boxes on a **real host** with `box` and
`rig` installed and run the duty lifecycle inside them. A crew builder session
runs *inside* a box, where that is both unavailable and prohibited: a nested
box stack claims the guest's own uplink subnet and breaks the guest's
networking (heavy-duty/box#80).

So this is not a leg a builder skipped. **No builder can run it**, by
construction; every crew drill to date was run by the operator on
`dan-office-workstation`, and `drills/0.1.0.md` names that host. The choice to
ship `0.1.1` on this record instead of on a real-host run is therefore the
maintainer's, and it is flagged as such on the release PR rather than settled
here.

## The measurement #321 §4 asks for

#321 §4 makes a lighter, **doors-unchanged** form available *"if the
release-path diff since `0.1.0` supports it"*. **It does not.** Measured at
the candidate ref against `7bb9f7aff751f497c7ef37caea2355c905045620`, the tree
`drills/0.1.0.md` records — **257 commits** apart:

| release door | since the `0.1.0` drilled tree |
| --- | --- |
| `.github/workflows/release.yml` | ceremony pin `0.4.0` → `0.5.0` (#298) |
| `.github/workflows/release-guards.yml` | all six guard pins `0.4.0` → `0.5.0` (#298) |
| `install.sh` | 1 line — the closing line reports the recorded provenance instead of a dead temp path (#222) |
| `shared/install.sh` | +34 lines — provisioning writes the box's git identity from its `gh` credential, or says why it could not (#294) |

And the engine surfaces the drill exercises are not unchanged either:

| surface | files changed since `7bb9f7a` |
| --- | --- |
| `shared/` | 21 |
| `cli/crew` | 1 file, +623 lines |
| `drill/` | 5 |
| `examples/` | 4 |
| `fleet-floor/` | 73 |

Stated as it reads, per #321 §4: **the doors-unchanged form is not available,
and a full drill is owed by this measurement.** It was not run, for the reason
above.

## What was run instead

Every leg of `shared-ci.yml`'s `check` job, run by hand at the candidate ref
in the release worktree on the `cndgrr` builder box (Debian 13 guest, `bash`
5.2, ShellCheck 0.10.0, node v22.23.2, Python 3). This is CI's own suite, not
a substitute of my own invention — but it is CI's suite, which is a strictly
weaker claim than the drill's, and the next section says exactly how.

It is run by hand because it does not run on this PR: `shared-ci.yml` carries
a paths filter (`shared/**`, `cli/**`, `drill/**`, `fleet-floor/**`,
`examples/**`, `install.sh`, `dist/**`), and a release PR touches none of them.
That is by design and it is what #208 did too — but it means the release head
carries no engine check at all unless somebody runs one and writes the numbers
down.

| Leg | Result |
| --- | --- |
| `bash -n` — 40 shell files | PASS |
| `shellcheck -x` — the full CI file list | PASS — 0 findings |
| `shared/test/run.sh` — the engine fixture suite | **passed 982, failed 0** |
| `shared/test/install-lifecycle.sh` — offline install lifecycle | **passed 28, failed 0** |
| `shared/test/artifact.sh` — the scp-able artifact, offline | **passed 56, failed 0** |
| `shared/test/install-drill.sh` — installer drill contract | **passed 26, failed 0** |
| `python3 -m py_compile fleet-floor/server/floor.py` | PASS |
| `fleet-floor/test/run.sh` with `FLOOR_TEST_REQUIRE_BROWSER=1` | WIP — the walk is running at this commit; filled in the next push |
| `fleet-floor/build.sh` — `index.html` matches `src/` | PASS — no diff |

**Failed: nothing in the legs that ran.** The browser half was *required*, not
permitted to skip: `FLOOR_TEST_REQUIRE_BROWSER=1` turns a missing driver or
browser into a failure, so the walk's result below is a real verdict and not
the silently-skipped UI test that reads exactly like a passing one.

## What this evidence cannot reach

This is the part a reader needs, because the table above is green and the drill
is the thing that was not run. Everything below is exercised by
`drill/rehearsal.sh` on a real host and by **nothing** in the list above:

- **The install on a real box.** The lifecycle and artifact suites drive the
  installer offline against a directory. They never provision a box, never arm
  cron, and never observe a box come up disarmed and say so.
- **The duty tick.** No session ran. Tick evidence at both ends of a run, the
  boot check, and the lock-contention exit are unexercised.
- **The whole authenticated phase 2** — the attention wake, the review round
  through both one-shot gates, head dedup, the re-request auto-approve, gate
  abuse. These need GitHub fixtures minted under the host identity.
- **`shared/install.sh`'s new git-identity write (#294)**, which is one of the
  two changed release doors above. Its success path needs a box that is already
  `gh`-authenticated; nothing here provides one.
- **Operator-config convergence against a real installed box.**
- **The fleet-floor console against real boxes.** The suite above drives a stub
  `box` CLI from a fixture; `drill/rehearsal-app.sh` is the half that runs the
  same assertions against a live fleet, and it did not run.
- **`crew hire` / `crew upgrade` on a live fleet**, which is how #294's and
  #220's operator-visible behaviour actually reaches an operator.

One thing that is *not* claimed as coverage here, deliberately: the fleet ran
this engine's lineage for two days and #311 reports it. That run is real
evidence about the boxes' **installed** engines, which is not this tree, and
folding it in as a leg would be exactly the "no evidence row for an exercise
that did not happen" that #233's round-1 correction refused. It is context, and
it is not in the table.

## The `0.1.0` coverage gap, unchanged

`drills/0.1.0.md` records nine skips across three roles — the creds-free half
of phase 1, unexercised because the boxes were already `gh`-authenticated — and
names #207 §1 and §3 as what covers it. `0.1.1` does not close that gap and
does not widen it; it inherits it, and #306 still holds it.

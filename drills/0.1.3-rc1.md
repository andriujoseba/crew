# Release drill — 0.1.3-rc1 — 2026-09-07

**No real-host drill round has been run for this candidate, and this record is
not a waiver.** That distinction is the first line because
[`drills/0.1.1.md`](0.1.1.md) — the only other record in this directory without
a round — *is* a waiver, and reading this one as another would invert what the
rc ladder is for. `0.1.1` decided not to drill. This candidate is cut **in
order to be drilled**: the round it is owed is **Gate A of
[heavy-duty/crew#659](https://github.com/heavy-duty/crew/issues/659)**, it is
named with its exact invocation below, and its readings land in this file
before the promotion to `0.1.3`.

`drills/README.md` states what the gate wants: *"The guard requires a **record**,
not a passing result"*, and it names the shape this candidate is in — *"the
first rc round still drills a **mutable** candidate. `drill-recorded` gates the
rc PR, so `drills/X.Y.Z-rc1.md` must exist before the merge that creates the
`X.Y.Z-rc1` tag — the record is committed against a tree that is not yet
tagged. That is heavy-duty/crew#490's problem, and it is unchanged here."*

## The candidate

Candidate ref: the tree of `release/659-0.1.3-rc1` at
`2412706`, cut from `main` at
`855bd701c973b287dfaff18cf24820eba423b882` (2026-09-07). The record commit that
adds this file changes this file and nothing else.

**What the cut changes, path by path**, so that the tree this record describes
is checkable from the PR's own diff rather than asserted:

| path | change | stamp path? |
| --- | --- | --- |
| `VERSION` | `0.1.3-dev` → the bare `0.1.3-rc1` | yes |
| `drills/0.1.3-rc1.md` | this file | yes |
| `shared/test/cli-reset.sh` | one fixture expectation de-literalised | **no** |

`CHANGELOG.md` and every file under `changelog.d/` are **byte-identical to the
merge base**, per the tag-only ladder: the candidate consumes no fragment and
stamps no section.

**The one non-stamp path is the cut's own breakage, and it is named here rather
than left to be found.** `reset-up-marks-every-roster-box` spelled
`CHECKPOINT_STALE=0.1.3-dev` on both sides of its expectation. The mark carries
the engine version — `cmd_up` passes `$ENGINE_VERSION` to
`checkpoint_stale_gate`, and `resolve_engine` sets that from this tree's
`VERSION` — so moving `VERSION` moved the value out from under a literal, and
the suite went red at the cut. The expectation now reads `VERSION`. Re-spelling
the literal would have fixed nothing for longer than one commit: the release
merge door re-arms `main` as `0.1.3-rc2-dev` automatically, in the same job as
the publish. `stamps-only.py` is vacuous on an rc tree and so does not object;
the path matters at the **promotion**, where the diff from this candidate's tag
to the final must touch stamps only — and this change is *inside* the tag, below
that anchor, so it is not in that diff.

## Why a round is owed, measured rather than asserted

The last tree drilled on a real host is `0.1.2`'s, recorded in
[`drills/0.1.2.md`](0.1.2.md) at
`crew@a877cd9731bb45da5085aff369c236663f203279` (2026-08-10).

- **788 commits** separate that tree from this candidate.
- **144 files** under `shared/`, `cli/`, `drill/`, `fleet-floor/`, `examples/`,
  `install.sh` and `dist/` differ between them.

That is the argument for the round, and it is also the argument against reading
the offline evidence below as a substitute for one. `0.1.1`'s waiver was
defensible because its engine diff from the drilled `0.1.0` tree was empty; this
candidate's is not, and nothing in this file claims otherwise.

## What *has* been measured on this tree

Offline suites only — every one of these runs in a container with no box, no
host and no fleet, which is exactly why none of them can stand in for a drill
leg. Run at the candidate ref above.

| suite | result |
| --- | --- |
| `shared/test/run.sh` | **passed 4859, failed 0** |
| `shared/test/install-lifecycle.sh` | **passed 66, failed 0** |
| `shared/test/artifact.sh` | **passed 106, failed 0** |
| `shared/test/install-drill.sh` | **passed 26, failed 0** |
| `shared/test/drill-teardown.sh` | **67 ok, 0 failed** |
| `fleet-floor/test/run.sh --no-browser` | **1049 ok, 1 skipped, 0 failed** |

The browser leg of `fleet-floor/test/run.sh` was **not** run here and its
absence is not coverage; `ci-floor` runs it on every PR and is the reading for
it.

## The declared leg inventory — every leg, not executed

`drills/README.md`: *"Every declared leg must appear in the round summary as
either executed or not executed with a named reason… A leg must never be absent
from the record."* No round has run, so every declared leg of
`drill/rehearsal-all.sh` is listed here as not executed with the same reason,
rather than omitted — an absent leg is what lets a gate be **credited to a
silence**, which is the failure Gate A2 exists to refuse.

| leg | state | reason |
| --- | --- | --- |
| `hygiene` | not executed | the Gate A round is owed (crew#659 A1) |
| `breaker` | not executed | the Gate A round is owed (crew#659 A1) |
| `resume` | not executed | the Gate A round is owed (crew#659 A1) |
| `attention` | not executed | the Gate A round is owed (crew#659 A1) |
| `attention-audit` | not executed | the Gate A round is owed (crew#659 A1) |
| `notify` | not executed | the Gate A round is owed (crew#659 A1) |
| `installer` | not executed | the Gate A round is owed (crew#659 A1) |
| `config` | not executed | the Gate A round is owed (crew#659 A1) |
| `app` | not executed | the Gate A round is owed (crew#659 A1) |
| `browser` | not executed | the Gate A round is owed (crew#659 A1) |
| `app-armed` | not executed | the Gate A round is owed (crew#659 A1) |
| `teardown` | not executed | the Gate A round is owed (crew#659 A1) |

This is a hand-written inventory transcribed from `DECLARED_LEGS` at
`drill/rehearsal-all.sh:194-197`, **not** the output of a round — no
`## declared leg states:` block appears above, because no round produced one.

## The round this file is waiting for

Gate A of crew#659, quoted as that issue carries it:

> `drill/rehearsal-all.sh --app-roster <path>` run on @danmt's drill host, where
> `<path>` names a roster carrying at least one **live armed member** — not a
> drill box — and the round's agreement block names at least one qualifying
> member with its measured delta and uncertainty, and the round's box census is
> unchanged by the second named-roster pass.

Two things about that invocation are recorded here because they decide whether a
round counts:

- **`--app-roster` is not optional.** The armed pass runs inside
  `[ "$APP" -eq 1 ] && [ -n "$APP_ROSTER" ]`, and the generated drill-roles pass
  can never qualify in its place: `rehearsal.sh` disarms cron on every box a
  round mints, while `agreement_armed_skewed()` requires `disarmed = False` and
  `tick_fresh = True`. The roster file is expected as a `*.roster.local` beside
  the drill.
- **The verdict is written either way.** `agreement_round_result()` returns
  `could-not-compare` on a zero armed count, so a round cannot print a false
  green — but crew#659 A2 requires the verdict, `compared` or
  `could-not-compare`, to be **read at this gate and written into this file**.
  A `could-not-compare` does not block promotion; it discharges the question
  rather than deferring it to a seventh round.

**Where a defect found by that round goes.** Not into a hand-edit of this file's
claims about the tree: crew#659 states that *"a defect found after the cut is a
re-cut, never a hand-edit"*, and `changelog-assembled` replays the merge base,
so the fix lands on `main` and ships as `0.1.3-rc2` with its own record at
`drills/0.1.3-rc2.md`. What is appended to *this* file is the round's readings —
`drills/` is a stamp path, so that append is admitted by `stamps-only.py` rather
than being the kind of edit it refuses.

# Release drill — 0.1.3-rc1 — 2026-09-07

**No real-host drill round has been run for this candidate, and this record is
not a waiver.** That distinction is the first line because
[`drills/0.1.1.md`](0.1.1.md) — the only other record in this directory without
a round — *is* a waiver, and reading this one as another would invert what the
rc ladder is for. `0.1.1` decided not to drill. This candidate is cut **in
order to be drilled**: the round it is owed is **Gate A of
[heavy-duty/crew#659](https://github.com/heavy-duty/crew/issues/659)**, it is
named with its exact invocation below, and its readings land in this file
before the promotion to `0.1.3`.

`drills/README.md` states what the gate wants: *"The guard requires a **record**,
not a passing result"*. What it demands before this merge is therefore this
file, not the round — and the round runs after the merge, against the published
`0.1.3-rc1` tag, with its readings appended here before the promotion.

That is triage's ruling of `2026-09-07` on crew#659, and **this PR carries the
correction to `drills/README.md` that it turns on**: the *"what it does not
buy"* paragraph conflated the record with the round, inferring from the true
premise *"the first rc round still drills a mutable candidate"* that the round
had to precede the merge. It no longer does, and it names the three grounds —
the candidate is what the fleet can install, the tag pins the SHA a branch
cannot, and the append is unobstructed because `drill-recorded` asserts nothing
on the `0.1.3-rc2-dev` tree the merge door re-arms `main` to. Its pointer to
crew#490 was spent as well; that issue closed `2026-08-26`.

## The candidate

Candidate ref: the tree of `release/659-0.1.3-rc1` at
`5711fa7736dee140c29a982b5e9a440c37104611`, cut from `main` at
`1778c6846bb3cde4cafa6214766905b01e2d4ca7` (2026-09-10). Every commit after
`5711fa7` on this branch touches `drills/` and nothing else, so the engine tree
this record describes is the engine tree being tagged — checkable as an empty
`git diff 5711fa7 HEAD -- shared/ cli/ drill/ fleet-floor/ examples/ install.sh
dist/`.

**That anchor was re-taken after a rebase, and the re-take belongs in the
record rather than behind it.** This branch was first cut from `main` at
`855bd70` with its engine boundary at `2412706`. `main` then moved to
`1778c68`, the branch was rebased onto it, and both of those SHAs were orphaned
— `2412706` is not an ancestor of this head. A drill record is a document
*about* a base ref, a commit range and a changed-path set, so carrying it across
a rebase byte-identical is precisely how it stops describing the tree it names:
the file was unchanged and every claim in this section became false at once.
Every figure below is re-measured at the anchor above, not relabelled.

**What the cut changes, path by path**, so that the tree this record describes
is checkable from the PR's own diff rather than asserted:

| path | change | stamp path? |
| --- | --- | --- |
| `VERSION` | `0.1.3-dev` → the bare `0.1.3-rc1` | yes |
| `drills/0.1.3-rc1.md` | this file | yes |
| `drills/README.md` | the record-vs-round correction, on triage's ruling | yes |

`CHANGELOG.md` and every file under `changelog.d/` are **byte-identical to the
merge base**, per the tag-only ladder: the candidate consumes no fragment and
stamps no section.

**Every path this cut touches is a stamp path — there is no non-stamp change to
argue about.** That is the strongest form this table can take, and it is not the
form it started in, so the reason is recorded here instead of the difference
being silently absent. The cut breaks a fixture by construction:
`reset-up-marks-every-roster-box` spelled `CHECKPOINT_STALE=0.1.3-dev` on both
sides of its expectation, and that mark carries the engine version — `cmd_up`
passes `$ENGINE_VERSION` to `checkpoint_stale_gate`, and `resolve_engine` sets
that from this tree's `VERSION` — so moving `VERSION` moves the value out from
under a literal. This branch carried the de-literalisation for a time. It no
longer does: [crew#706](https://github.com/heavy-duty/crew/pull/706) landed the
same repair on `main` (`3dc97bb`, `7352dda`, `24b7ffb`), and this candidate
**inherits** it through the base named above rather than making it. The breakage
is real and the fixture that absorbs it is in the tagged tree — it is simply not
in this PR's diff. `shared/test/cli-reset.sh` reads **271 passed, 0 failed** at
this head.

## Why a round is owed, measured rather than asserted

The last tree drilled on a real host is `0.1.2`'s, recorded in
[`drills/0.1.2.md`](0.1.2.md) at
`crew@a877cd9731bb45da5085aff369c236663f203279` (2026-08-10).

- **792 commits** separate that tree from this candidate, counted at the
  `5711fa7` named above rather than at the branch's first commit. The same count
  taken at the branch head reads **796**; the four-commit difference is exactly
  the four `drills/`-only commits that follow the anchor, and neither figure
  corrects the other. The anchor's is the one quoted because it is the tree
  being tagged.
- **144 files** under `shared/`, `cli/`, `drill/`, `fleet-floor/`, `examples/`,
  `install.sh` and `dist/` differ between them — 144 at the anchor and 144 at
  the head, since nothing under those paths moves after it.

That is the argument for the round, and it is also the argument against reading
the offline evidence below as a substitute for one. `0.1.1`'s waiver was
defensible because its engine diff from the drilled `0.1.0` tree was empty; this
candidate's is not, and nothing in this file claims otherwise.

## What *has* been measured on this tree

Offline suites only — every one of these runs in a container with no box, no
host and no fleet, which is exactly why none of them can stand in for a drill
leg. **Every reading below was re-run on the rebased tree**, not carried across
the rebase and relabelled: `main`'s own reset-fixture commits now sit under
`shared/test/`, so a reading taken before the rebase covers a different tree
than the one being tagged. One suite is also named that this table did not
carry before, because it is the one the rebase moved.

| suite | result |
| --- | --- |
| `shared/test/run.sh` | **passed 4859, failed 0** |
| `shared/test/cli-reset.sh` | **passed 271, failed 0** |
| `shared/test/install-lifecycle.sh` | **passed 66, failed 0** |
| `shared/test/artifact.sh` | **passed 106, failed 0** |
| `shared/test/install-drill.sh` | **passed 26, failed 0** |
| `shared/test/drill-teardown.sh` | **67 ok, 0 failed** |
| `fleet-floor/test/run.sh --no-browser` | **1049 ok, 1 skipped, 0 failed** |
| `fleet-floor/test/run.sh` (browser leg included) | **1059 ok, 1 skipped, 0 failed** |

The suite's own browser leg **was** run, as a second pass with the leg enabled:

    PW_CHROME=/usr/bin/chromium FLOOR_TEST_REQUIRE_BROWSER=1 fleet-floor/test/run.sh

`FLOOR_TEST_REQUIRE_BROWSER=1` is what makes that a reading rather than a
formality — under it a missing driver or missing browser is a **failure**, not a
skip, so the pass cannot be satisfied by the leg quietly not running. It
reported `mode: LIVE`, `browser walk: asserted 153 checks (>= 96)` and
`browser walk (DEMO mode): asserted 21 checks (>= 14)`. The single skip is the
same one the `--no-browser` pass reports — *"drill: `--drill-roles` parses and
reaches the host check — a real box CLI is on PATH"* — and is not a browser
skip.

**Two things that reading is not.** It is not a `ci-floor` result: `ci-floor.yml`
filters its `pull_request` trigger to
`paths: ['fleet-floor/**', '.github/workflows/ci-floor.yml']`, and this cut
touches `VERSION` and `drills/` only, so **no `ci-floor` check run appears at
this head at all**. The same filter excludes `ci-shell`, whose paths are
`shared/**`, `cli/**`, `drill/**`, `fleet-floor/**`, `examples/**`,
`install.sh`, `dist/**` and `.ceremony/**`: a stamps-only cut matches none of
them, so the shell suite has no CI reading on this candidate either, and the
table above is what stands in its place. And it is not the `browser` leg of the
drill, which is listed as not executed in the inventory below and stays there:
that leg is a read-only walk *against the real fleet*
(`drill/rehearsal-all.sh:566`), whereas this pass drives a local server and its
fixtures. The two share a name and nothing else, and neither one may be credited
to the other.

## The declared leg inventory — every leg, not executed

`drills/README.md`: *"Every declared leg must appear in the round summary as
either executed or not executed with a named reason… A leg must never be absent
from the record."* No round has run, so every declared leg of
`drill/rehearsal-all.sh` is listed here as not executed with the same reason,
rather than omitted — an absent leg is what lets a gate be **credited to a
silence**, which is the failure Gate A2 exists to refuse.

| leg | state | reason |
| --- | --- | --- |
| `hygiene` | not executed | the Gate A round is owed (crew#659 A1) |
| `breaker` | not executed | the Gate A round is owed (crew#659 A1) |
| `resume` | not executed | the Gate A round is owed (crew#659 A1) |
| `attention` | not executed | the Gate A round is owed (crew#659 A1) |
| `attention-audit` | not executed | the Gate A round is owed (crew#659 A1) |
| `notify` | not executed | the Gate A round is owed (crew#659 A1) |
| `installer` | not executed | the Gate A round is owed (crew#659 A1) |
| `config` | not executed | the Gate A round is owed (crew#659 A1) |
| `app` | not executed | the Gate A round is owed (crew#659 A1) |
| `browser` | not executed | the Gate A round is owed (crew#659 A1) |
| `app-armed` | not executed | the Gate A round is owed (crew#659 A1) |
| `teardown` | not executed | the Gate A round is owed (crew#659 A1) |

This is a hand-written inventory transcribed from `DECLARED_LEGS` at
`drill/rehearsal-all.sh:194-197`, **not** the output of a round — no
`## declared leg states:` block appears above, because no round produced one.

## The round this file is waiting for

Gate A of crew#659, quoted as that issue carries it:

> `drill/rehearsal-all.sh --app-roster <path>` run on @danmt's drill host, where
> `<path>` names a roster carrying at least one **live armed member** — not a
> drill box — and the round's agreement block names at least one qualifying
> member with its measured delta and uncertainty, and the round's box census is
> unchanged by the second named-roster pass.

⚠️ **The invocation is quoted above as #494 wrote it, and it must be read under
crew#659's `2026-09-07` amendment: the round is run with `--ref 0.1.3-rc1`.**
The quoted criterion is deliberately not re-worded — a relocated criterion that
drifts is one nobody agreed to — but taken bare it is now wrong in a way that
would not announce itself. `drill/rehearsal-all.sh:57` reads
`INSTALL_REF="${CREW_DRILL_REF:-main}"`, and the merge door re-arms `main` as
`0.1.3-rc2-dev` the instant this cut lands, so a round with no `--ref`, taken
when this gate is read, drills a tree that is **not this candidate** — and it
would say so only in the summary's `## drilled source: <sha> (remote … ref
main)` line, which nothing on the gate asks anybody to read. **The round this
file is waiting for is therefore:**

    drill/rehearsal-all.sh --ref 0.1.3-rc1 --app-roster <path>

A tag resolves through the same phase-0 path a branch does and lands on the
record as the resolved SHA, which is what crew#490 was built for.

Two further things about that invocation are recorded here because they decide
whether a round counts:

- **`--app-roster` is not optional.** The armed pass runs inside
  `[ "$APP" -eq 1 ] && [ -n "$APP_ROSTER" ]`, and the generated drill-roles pass
  can never qualify in its place: `rehearsal.sh` disarms cron on every box a
  round mints, while `agreement_armed_skewed()` requires `disarmed = False` and
  `tick_fresh = True`. The roster file is expected as a `*.roster.local` beside
  the drill.
- **The verdict is written either way.** `agreement_round_result()` returns
  `could-not-compare` on a zero armed count, so a round cannot print a false
  green — but crew#659 A2 requires the verdict, `compared` or
  `could-not-compare`, to be **read at this gate and written into this file**.
  A `could-not-compare` does not block promotion; it discharges the question
  rather than deferring it to a seventh round.

**Where a defect found by that round goes.** Not into a hand-edit of this file's
claims about the tree: crew#659 states that *"a defect found after the cut is a
re-cut, never a hand-edit"*, and `changelog-assembled` replays the merge base,
so the fix lands on `main` and ships as `0.1.3-rc2` with its own record at
`drills/0.1.3-rc2.md`. What is appended to *this* file is the round's readings —
`drills/` is a stamp path, so that append is admitted by `stamps-only.py` rather
than being the kind of edit it refuses.

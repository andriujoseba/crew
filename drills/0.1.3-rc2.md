# crew 0.1.3-rc2 drill record — cut 2026-09-11

This is the record required before the candidate cut, not a claim that the
real-host round has already run. The round runs after merge against the
published `0.1.3-rc2` tag, and its readings are appended here before
promotion. This is not a waiver: crew#709 Part 3 names the round and Part 4
names the promotion gate it feeds.

## The candidate

Candidate engine anchor: `build/709-rc2-cut` at
`b071240fc7b12aebb4806883a6f3f525752dffbd`, cut from `main` at
`283e442917eebd51f8bc27bc66b1a778786f7644` (2026-09-11). Every commit
after that anchor changes only this record, so `git diff b071240 HEAD --
':!drills/0.1.3-rc2.md'` is empty and the engine tree named here cannot drift
as its evidence is completed. The release doors publish the final merge tree
as the prerelease tag `0.1.3-rc2` and re-arm `main` as `0.1.3-rc3-dev`.

The tree drilled for the preceding candidate is the published
`0.1.3-rc1` tag at `b14f4aa569f3085d232fb5872814e2960d91cf95`.
The engine-tree diff from that immutable anchor is the following 17 paths.
The issue citations identify the prerequisite whose merged tree supplies
each change.

| path | change since `0.1.3-rc1` |
| --- | --- |
| `drill/install-borrowed-box.sh` | add state capture and restoration for a borrowed drill box (#712) |
| `drill/install-drill.sh` | drive and verify borrowed-box restoration (#712) |
| `drill/rehearsal-all.sh` | restore and validate the borrowed box after Section A (#712) |
| `drill/rehearsal-safety.sh` | census outside attention demands and read this round across log rotation (#714) |
| `drill/rehearsal.sh` | make outside attention a census while retaining the sandbox safety refusal (#714) |
| `fleet-floor/test/browser.js` | accept prerelease integrity stamps and gate fixture-only assertions (#711) |
| `fleet-floor/test/cli.sh` | derive the fixture stamp from its version (#711) |
| `fleet-floor/test/floor/actions.sh` | bound the age of the restart-wedge verdict under test (#701) |
| `fleet-floor/test/run.sh` | cover prerelease engine verdicts (#711) |
| `fleet-floor/test/stub-box` | expose the prerelease fixture version (#711) |
| `shared/docs/rehearsal.md` | document the attention census and rotation-safe phase-2 read (#714) |
| `shared/lib/common/identity.sh` | keep `gh_identity` standard output to the login alone (#708) |
| `shared/test/builder.sh` | cover the fleet-wide attention census call (#714) |
| `shared/test/common.sh` | anchor notify wiring on the attention census (#714) |
| `shared/test/common/identity.sh` | pin identity output and first-tick logging (#708) |
| `shared/test/drill.sh` | exercise attention census, log rotation and borrowed-box restoration (#712, #714) |
| `shared/test/install-drill.sh` | exercise borrowed-box restoration in the installed drill (#712) |

The non-engine delta from the preceding tag is deliberately separate:

- `VERSION` moves from the re-armed `0.1.3-rc2-dev` value on `main` to the
  bare `0.1.3-rc2` candidate stamp in this cut.
- `drills/0.1.3-rc1.md` contains the issue-driven append of the rc1 round's
  four passes (#715).
- This file records the rc2 candidate and the round it is owed.

`CHANGELOG.md` and every file under `changelog.d/` are byte-identical to the
merge base, as the tag-only candidate ladder requires. The first `## ` section
of `CHANGELOG.md` is `## 0.1.2 — 2026-08-10`, not a stamped rc section.

## Offline verification at the candidate anchor

The offline suites exercise the candidate tree without a box host or live
fleet. They do not substitute for the real-host round below.

| suite | result |
| --- | --- |
| `shared/test/run.sh` | **passed 4982, failed 0** |
| `fleet-floor/test/run.sh --no-browser` | **1049 ok, 1 skipped, 0 failed** |
| `PW_CHROME=/usr/bin/chromium FLOOR_TEST_REQUIRE_BROWSER=1 fleet-floor/test/run.sh` | **1059 ok, 1 skipped, 0 failed** |

The browser-required environment makes a missing driver or browser a failure,
not a skip. The one skip in the no-browser pass is the expected real-host
precondition: `--drill-roles` needs a real box CLI on `PATH`.

The release and scope gates were run locally against this tree at ceremony
pin `0.7.7` (`fc6325e762cf91d9a5723a82803d2f03059007a1`):

| guard | result |
| --- | --- |
| `changelog-armed` | rc fragment-mode state agrees with `0.1.3-rc2`; fragments survive |
| `changelog-monotonic` | all 3 merge-base release headings remain |
| `changelog-assembled` | no fragments consumed; `CHANGELOG.md` is byte-identical to base `283e442` |
| `drill-recorded` | this non-empty rc2 record exists |
| `runner-isolated` | 8 workflows scanned; no unvouched self-hosted PR-code runner |
| `docs-sync` | 6 vendored files exactly mirror ceremony `0.7.7` |
| `stamps-only` | 76 ok, 0 failed; a candidate carries only its own record |
| `scope-coverage` | 36 ok, 0 failed; 598 tracked paths covered by 13 rows and 1 declared exception |

## Declared leg inventory — every leg not yet executed

Every independently runnable leg declared by `DECLARED_LEGS` in
`drill/rehearsal-all.sh` is present. None has run against the rc2 tag because
the tag does not exist until this cut merges; that named ordering is the
reason for every not-executed state, not an omission and not a waiver.

| leg | state | reason |
| --- | --- | --- |
| `hygiene` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `breaker` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `resume` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `attention` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `attention-audit` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `notify` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `installer` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `config` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `app` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `browser` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `app-armed` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |
| `teardown` | not executed | the post-publication rc2 round is owed (crew#709 Part 3) |

This inventory is transcribed from the declaration at cut time. The round's
generated `## declared leg states:` block is appended with its readings; no
generated block is invented before the instrument runs.

## The round this file is waiting for

On the drill host, with a roster file carrying at least one live armed member:

    drill/rehearsal-all.sh --ref 0.1.3-rc2 --app-roster <path>

The fresh pass must show `pre-auth: login WARN logged` passing after one
explicit tick (#708), and the real-fleet browser leg must execute with any
non-ok row reported only as a skip with a named reason (#711). On the
authenticated `--reuse` pass, Section A must leave no borrowed drill box
armed (#712), and phase 2 must run on the operator's own identity while its
outside attention demands are reported as suppressed (#714). The agreement
verdict, `compared` or `could-not-compare`, is recorded either way.

The same operator sitting then records crew#709's Gate B sequence and starts
Gate C's week-scale reading. A negative reading is recorded rather than
hidden; the issue decides whether it blocks promotion or mints the next
candidate defect.

## The round — 2026-09-12

**Host:** `dan-office-workstation`, agent `claude`; the host `box` and guest
`rig` versions are pending the operator's write-time reading and are not
inferred from the rc1 record. **Candidate ref:** tag `0.1.3-rc2`, resolved
`2a8f40e24ac10e7ff7641ac31fd36bfe35bcec1e` on every pass (`## drilled
source:` line).

The round ran three times and changed box identity between its second and
third passes. No pass supplied `--app-roster`: the host carried no live armed
member, so the agreement verdict was `could-not-compare` throughout. Phase 2
had never ticked in this window before pass 2; rc1's record names every role
loop as unproven.

| pass | invocation | box identity | outcome |
| --- | --- | --- | --- |
| 1 | `drill/rehearsal-all.sh --ref 0.1.3-rc2` | none (creds-free) | phase 2 skipped on all three roles |
| 2 | `drill/rehearsal-all.sh --ref 0.1.3-rc2 --reuse` | `danmt` | phase 2 ticked for the first time in this window; builder and reviewer loops were unrunnable, and the operator stopped the run during the reviewer leg |
| 3 | `drill/rehearsal-all.sh --ref 0.1.3-rc2 --reuse` | `dan-claude-bot` | all three role loops ran and supplied the candidate's four required readings |

### The candidate's four required readings

| criterion | reading | pass |
| --- | --- | --- |
| `pre-auth: login WARN logged` passes on fresh boxes after one explicit tick (#708) | **ok**, on genuinely creds-free boxes; the reused passes skip this row | 1 |
| the `browser` leg executes, with every non-ok row a skip carrying a named reason (#711) | **executed: 56 ok, 1 skipped, 0 failed**; the skip is `#486 force/mode fixture assertions — real fleet has no guaranteed ff-wedged box` | 3 |
| Section A leaves no borrowed drill box armed on the authenticated reuse pass (#712) | **ok** — `Section A returned crew-drill-triage disarmed` | 3 |
| phase 2 runs as the operator identity and suppresses outside `attention` demands (#714) | **ok on all three roles** — 3 demands (`heavy-duty/incubator#468`, `#469`, `#470`) were censused, reported `seen and suppressed`, and drew no pickup | 2 |

The #714 reading belongs to pass 2, the only pass authenticated as the
operator. Pass 3 censused zero outside demands and therefore cannot supply
that reading.

### Pass 3 — all role loops ran

| leg / role | reading |
| --- | --- |
| triage | **77 ok, 4 skipped, 7 failed** — all seven failures are `breaker` |
| builder | **106 ok, 4 skipped, 2 failed** — both failures are `resume` |
| reviewer | **64 ok, 4 skipped, 7 failed** — all seven failures are `breaker` |
| hygiene | ok on all three roles — preservation and refusal |
| attention | ok — dispatch without code and timeout report |
| attention-audit | ok — both malformed shapes reported, not repaired, with an alert on transition |
| breaker | **FAIL** — passed on builder and failed on triage and reviewer (#724) |
| resume | **FAIL** — pending head resumed before check conclusion; unchanged head did not stop at the installed zero-action threshold (#725) |
| notify | **INCOMPLETE** — operator channel unreachable on this host: `no-credentials`; the union is unproven |
| installer (Section A, `crew-drill-triage`) | **PASS**; the box was returned disarmed |
| config | **10 ok, 0 failed** |
| app | **36 ok, 5 skipped, 0 failed**; all three unauthenticated `agree` artefacts from pass 1 pass here |
| browser | **56 ok, 1 skipped, 0 failed** |
| app-armed | `could-not-compare` — no `--app-roster` and no live armed member |
| teardown | kept — the round was not green and the boxes were left standing for inspection |

The instrument printed this declared-leg block on pass 3:

```
## declared leg states:
## leg executed hygiene  (ok; preservation + refusal)
## leg executed breaker  (FAIL)
## leg executed resume  (FAIL; pending head resumed before check conclusion)
## leg executed attention  (ok; dispatch without code + timeout report)
## leg executed attention-audit  (ok; both shapes reported, not repaired, alerts on transition)
## leg not-executed notify  (INCOMPLETE; leg skipped: operator channel unreachable: no-credentials — union UNPROVEN)
## leg executed installer  (ok; Section A record emitted)
## leg executed config  (ok; operator mode + registry contract)
## leg not-executed app  (INCOMPLETE; agreement could-not-compare: no armed, ticking, clock-skewed box)
## leg executed browser  (ok; read-only browser walk executed)
## leg not-executed app-armed  (skip; not requested: no --app-roster; requires an armed member)
## leg not-executed teardown  (kept; round not green — boxes LEFT STANDING to inspect)
## section states: 6 passed, 5 failed, 4 skipped/not-run
```

### Pass 2 — operator identity exposed two harness prerequisites

| leg / role | reading |
| --- | --- |
| triage | **85 ok, 4 skipped, 7 failed** — all seven failures are `breaker` |
| builder | **70 ok, 16 skipped, 23 failed** — no session started anywhere in its `duty.log` |
| reviewer | stopped by the operator during the review round after `gh: Review cannot be requested from pull request author. (HTTP 422)` |
| attention census | **3 demands censused and all three asserted suppressed on every role** |

The builder needed a fork of its freshly minted sandbox, but the round created
none (#727). The operator identity also cannot fork its own sandbox or review
its own pull request; phase 2 must reject that identity before starting
(#729). These are recorded readings, not discarded attempts.

### Pass 1 — fresh and creds-free

`pre-auth: login WARN logged` passed on reviewer with **17 ok, 3 skipped, 0
failed** and an `INCOMPLETE` exit because phase 2 was skipped. The per-role
figures for triage and builder were not captured in the transcript available
when this record was written and are not reconstructed.

Section A passed and returned its box disarmed. `config` reported **10 ok, 0
failed**. `app` reported **23 ok, 12 skipped, 3 failed**: its three `agree`
rows were a creds-free artefact and pass after authentication in pass 3.
`browser` did not execute because `playwright-core` was not installed on the
host. `app-armed` was not requested and its verdict was `could-not-compare`.
Teardown kept the boxes.

### Findings and gate reading

Every finding was a first reading because rc1 never reached phase 2:

| issue | finding | class |
| --- | --- | --- |
| #724 | `breaker` graded ticks it never confirmed ran | harness |
| #725 | `resume` acted on a pending head and missed the zero-action boundary | **engine** |
| #726 | an inherited lock descriptor was reported as a previous run still running | **engine**, low severity |
| #727 | the builder leg required a sandbox fork the round never created | harness |
| #728 | authored-PR awareness searched all of GitHub | **engine** |
| #729 | phase 2 accepted a box identity equal to the host identity | harness |

The round did **not** pass. The engine findings move installed modules, so
`0.1.3-rc3` is a real candidate rather than a harness-only re-cut. Gate B and
Gate C must be read against that successor candidate, not credited from
`0.1.3-rc2`.

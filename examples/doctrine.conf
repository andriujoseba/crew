# doctrine.conf — operator-owned paths named in agent prompts.
#
# Paths are interpreted by the agent inside each governed repository.
# shellcheck shell=bash disable=SC2034

DOCTRINE_ENTRYPOINT="AGENTS.md"
DOCTRINE_TRIAGE="TRIAGE.md"
DOCTRINE_BUILDER="BUILDER.md"
DOCTRINE_REVIEWER="REVIEWER.md"

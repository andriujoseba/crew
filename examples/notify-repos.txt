# notify-repos.txt — the operator notifier's sweep list. One owner/repo per
# line; # and blanks ignored.
#
# This list must stay WIDER than the triage repos.txt: a cross-repo handoff
# is exactly the case the operator cannot discover on their own (rig#112
# sat state:needs-human nine hours because the old notifier swept ceremony
# alone). Every repo the fleet can put a state:needs-human PR in belongs
# here — org repos AND the bot forks.
#
# Explicit list, not org discovery: ~34 API calls per 5 minutes for a
# few-times-a-day event is the wrong trade, and deriving repos from prose
# produced false positives before.
heavy-duty/ceremony
heavy-duty/incubator
heavy-duty/rig
heavy-duty/box
heavy-duty/cast
#
# The fork lines run AHEAD of the forks on purpose. A repo that does not
# exist yet is inert here — the sweep's `gh pr list` fails, stderr is
# discarded and the loop `continue`s to the next repo (notify.sh) — so a
# line costs nothing until its fork is minted and covers it the moment it
# is. A list that lags the fork is rig#112 again.
dan-claude-bot/incubator
claude-bot-andresmgsl/incubator
cndgrr/incubator
andriujoseba/incubator

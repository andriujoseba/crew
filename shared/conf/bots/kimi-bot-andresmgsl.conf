# bot.conf — kimi-bot-andresmgsl: reviewer.
# shellcheck shell=bash disable=SC2034

BOT_ROLES="reviewer"
BOT_PATH_PREPEND="$HOME/.kimi-code/bin"

BOT_CLI_CMD=(kimi -p)

bot_cli_probe() {
  kimi doctor >/dev/null 2>&1
}

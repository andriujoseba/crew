# role profile: triage — the shape of the work, agent-agnostic.
# Sole issue-minter; also the box that runs the operator notifier.
#
# Session budgets calibrated from fleet metrics (triage median 424s max
# 946s; mentions max 1062s; hygiene max 683s).
# shellcheck shell=bash disable=SC2034

TIMEOUT_TRIAGE=1500
TIMEOUT_MENTION=1500
TIMEOUT_HYGIENE=3000

# Model TIER, per duty (#469) — the third statement about a duty's shape,
# beside the timeout above and the budget below, and here for the same reason
# they are: these are facts about what kind of work this duty is, and splitting
# them across files is how they drift apart (D2).
#
# Unset means "use the agent profile's own BOT_CLI_CMD", and unset is what
# ships (D4): with these empty every session is invoked and behaves exactly as
# it did before this existed. The log is the one deliberate exception —
# SESSION END gains `tier=default` on unconfigured lines too, by D5, so an
# aggregate can always read what a session was bought at. The value is passed
# through to the
# agent profile, which owns the translation into its own flag — so what goes
# here is whatever that CLI calls the tier, and a profile that cannot express
# one leaves the duty on the default and warns (D3).
#
# THE TIERS ARE @danmt's, and they are deliberately not set here. #469 ships
# the lever and explicitly declines to pull it: this is the one change in the
# duty.log chain that alters the quality of work that SHOULD happen rather
# than removing work that should not, and a hygiene sweep that misreads a
# label writes a wrong one.
#
# What the recommendation was, recorded so the decision has its measurement to
# hand (#469 D6). From this role's own box, 2026-08-24: mention 2399 sessions
# at a 1.57-minute mean — recognition work on a narrow payload — and hygiene
# 1606, confirmation against a board. Those are the candidates, and they are
# the candidates AFTER #465 and #466 cut their volume, which is the point:
# tier what remains, not what is about to be deleted. TRIAGE IS THE ONE TO
# LEAVE AT THE TOP TIER — it writes the contract every builder and reviewer
# downstream depends on, and at 3.2% of sessions tiering it would save almost
# nothing and risk the most.
MODEL_TRIAGE=
MODEL_MENTION=
MODEL_HYGIENE=

# Session BUDGETS (#464) — the volume bound, beside the per-session bound the
# three timeouts above give. A timeout caps one session; these cap how many
# and how long, per kind, in the rolling BUDGET_WINDOW from
# fleet.defaults.conf. Either ceiling alone arms the lane; 0 is OFF, and off
# is what ships.
#
# THE NUMBERS ARE @danmt's, and they are deliberately not set here. What a
# first set should be calibrated against is the 2026-08-24 measurement on this
# very role's box: hygiene 1606 sessions / 137.8 h, mention 2399 / 62.9 h,
# triage 131 / 22.1 h in one day. The obvious shape is a multiple of the
# HONEST need — and the honest need for hygiene and mention is not yet known,
# because #465 and #466 are what establish it. Setting a ceiling from the
# figures above would enshrine the leak as the budget.
BUDGET_SESSIONS_TRIAGE=0
BUDGET_MINUTES_TRIAGE=0
BUDGET_SESSIONS_MENTION=0
BUDGET_MINUTES_MENTION=0
BUDGET_SESSIONS_HYGIENE=0
BUDGET_MINUTES_HYGIENE=0

# Maximum fresh notification threads carried by one tick-wide mention session.
# Any remainder stays outside the success commit and re-wakes next tick.
MENTION_THREAD_CEILING=50

# Hygiene sweep interval — self-scheduling inside the duty tick, so a
# skipped top-of-hour tick delays it instead of losing the hour.
HYGIENE_INTERVAL=3600

# Hygiene sweep FLOOR — the longest a repo may go without a session while its
# board sits unchanged (#465). The interval above says how often the gate
# LOOKS; this says how often a repo sweeps regardless of what it sees, and it
# is what keeps the change gate a saving rather than a hole: a 48h reclaim, a
# 7-day post-merge nudge and an issue the world made obsolete all turn on the
# clock and not on the board. On a static board this takes the cost from 24
# sessions a day to 2 and leaves the unconditional backstop intact.
# Kept equal to HYGIENE_FLOOR_DEFAULT in shared/lib/duty-hygiene.sh.
HYGIENE_FLOOR=43200

# Box resources for `crew new` — light: the triage box reads boards and
# launches short sessions. It carries the Telegram credentials for notify;
# repos.txt is the AUTHORITATIVE registry for this role.
BOX_CPU="2"
BOX_MEMORY="4GiB"
BOX_DISK="30GiB"

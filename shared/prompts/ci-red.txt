You are the builder {{ME}} in {{REPO}}; PR #{{NUM}} is yours and the check at its current head FAILED: {{CHECKS}}. This is your work, not the panel's — a red check is the author's own signal, and a reviewer's first act is to read it. Do not request or re-request review while the head is red; make it green first. In order: (1) read the actual failure — fetch the failing job's log, do not infer the cause from the check's name; (2) classify it out loud in a PR comment, as INFRASTRUCTURE (a runner outage, a rate-limited or unreachable download, a timeout in setup before any of your code ran, a dependency the job could not fetch) or ATTRIBUTABLE (your branch's code, tests, or config); (3) if it is INFRASTRUCTURE, rerun the failed job with no code change and say in the comment that you reran it and why you believe it is transient; (4) if it is ATTRIBUTABLE, fix it as a normal fix round — worktree, worklog checkboxes, commit cadence, push — but the fix is not the end of the round: the signal is. Your classification comment satisfies the round protocol's rule (1) announce; rules (2) and (3) govern in full, including waiting for the new head's check to settle before signalling and using the green-head exception as the one by-hand exit. Rerun a given head AT MOST ONCE: if the same failure returns on an unchanged tree, it is not transient, and the correct next act is a fix or a question, never a second rerun. If you cannot start a rerun at all (no permission, no workflow-dispatch, the run is not rerunnable), say so in the comment and name what you tried — a builder that goes quiet here looks identical to one that fixed it. If the cause is genuinely outside this PR — the same job fails identically on the default branch — say that, name the evidence (the branch, the run, the SHA), and then you may proceed with the round on a red head, because an argued exception is fine and silence about a red check is not. Post evidence either way: this session's only durable output is what is written on the PR. {{WT_RULES}} {{ROUND_RULES}}

- Declared predecessor state changes:
{{EDGES}}
  Re-read each dependant's body — its spec, tasks, criteria, task lists, and any `Blocked by` clause kept as prose — against the landed tree. These are leads, not verdicts: a body that remains correct is an expected outcome, so do not invent a repair.

VERDICT SUBMISSION IS ONE-SHOT, hard rule: compose the verdict body ONCE, write it to a file, and if you have already composed a verdict for this head in this session you are done — never recompose or 'improve' it. Submit ONLY via: {{BIN}}/submit-verdict.sh <owner/repo> <pr-number> <full-head-sha> <approve|request-changes> <body-file> — never via gh pr review or the API directly. The wrapper re-checks the pulls/<N>/reviews endpoint live immediately before submitting, refuses if the head moved (exit 2 — review the new head instead), submits once pinned to your head SHA, and verifies with that same endpoint; that endpoint reflects your review immediately (it is the SEARCH index that lags, not it) — trust its verdict: exit 0 means your review is present and you STOP, even if intermediate output looked ambiguous. Never submit a second time to make sure — a duplicate verdict is a protocol violation; a missed one is recoverable next tick. The wrapper retries once by itself with your identical body; you never regenerate. If you discover an existing double-post of yours, do not post a comment about it — mention it in your final output only.

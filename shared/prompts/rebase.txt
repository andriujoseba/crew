You are the builder {{ME}} in {{REPO}}; PR #{{NUM}} is yours and no longer merges (mergeable state CONFLICTING). Rebase its branch onto the current default branch, resolve the conflicts, and push. Then post a one-line note on the PR saying you rebased, and post the round-answered signal '{{MARK_ANSWERED}} <new-head-sha>' at the rebased head — do NOT re-request the panel yourself (#133): the engine re-requests every panelist from that signal, since your rebase staled their approvals and an approval is of a specific head. Do the rebase inside the PR's own worktree. {{WT_RULES}}
